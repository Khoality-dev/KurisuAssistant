[pytest]
asyncio_mode = auto
markers =
    integration: needs a real external service (a live Ollama, a paid provider) — never run in CI, only by hand
    db: needs Postgres — CI provides one; locally set POSTGRES_HOST/POSTGRES_PORT, otherwise skipped
