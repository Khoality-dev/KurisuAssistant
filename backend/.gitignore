# Python
venv/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/

# Environment
.env

# Data (auto-created at runtime)
data/
userdata/

# Secrets & certs
nginx/certs/

# Model artifacts
whisper-finetuned/

# Config overrides
configs/default.json
mcp_config.json

# One machine's Compose extras — a GPU reservation, an existing reverse-proxy
# network. Compose loads this automatically; it describes a host, not the
# project, which is why it is not committed. See docs/development.md.
docker-compose.override.yml

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
nul

# Docker
postgres-data/
ollama/
openwebui/

# Dev checkout: symlink to docker-compose.dev.yml (see that file)
compose.yaml
