POSTGRES_USER=kurisu
POSTGRES_PASSWORD=kurisu
POSTGRES_HOST=postgres-container
POSTGRES_PORT=5432
POSTGRES_DB=kurisu

# Account creation. Closed by default: a fresh server is seeded with an
# `admin` account, so open registration is not needed to get started, and
# leaving it open hands an account to anyone who can reach the port.
ALLOW_REGISTRATION=false

# Brute-force protection on /login and /register, per client address.
# Set AUTH_RATE_LIMIT_MAX_ATTEMPTS=0 to disable.
AUTH_RATE_LIMIT_MAX_ATTEMPTS=10
AUTH_RATE_LIMIT_WINDOW_SECONDS=300
