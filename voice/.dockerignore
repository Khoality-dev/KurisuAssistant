# The image carries the package and nothing else. The model cache is a volume,
# the tests and docs are not part of the running service.
data/
tests/
docs/
*.md
**/__pycache__/
**/*.py[cod]
.pytest_cache/
docker-compose*.yml
