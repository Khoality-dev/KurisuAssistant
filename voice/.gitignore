# The model cache: Whisper models pulled and converted at runtime, and the
# synthesis weights. Docker mounts a volume here; locally it is whatever you
# pulled. Never source.
data/

__pycache__/
*.py[cod]
.pytest_cache/
venv/
.venv/
