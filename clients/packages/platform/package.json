{
  "name": "@kurisu/platform",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "What a host lends the renderer, behind one interface. Electron answers yes to most of it; a browser answers no.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@kurisu/models": "*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^2.1.9"
  }
}
