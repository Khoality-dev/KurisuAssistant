{
  "name": "@kurisu/models",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The wire protocol as types and runtime data. The backend is the authority; nothing here may depend on a UI, a platform or a transport.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^2.1.9"
  }
}
