{
  "name": "@kurisu/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The server, as this client calls it: REST, the WebSocket and its handshake, tokens, and the file sources behind them.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {"@kurisu/models":"*","@kurisu/platform":"*","axios":"^1.6.5"},
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^2.1.9"
  }
}
