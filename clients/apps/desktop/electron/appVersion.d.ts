/**
 * `__APP_VERSION__` is a Vite `define` (see vite.config.ts): the desktop
 * package.json's version, substituted at build time into the main process, the
 * preload and the renderer alike (#257).
 */
declare const __APP_VERSION__: string;
